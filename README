本分支基于dev-2.3.1分支进行优化，目的是为了降低compaction delete的处理成本和耗时；
之前数据点越密集compaction delete耗时越长且成本越高，现在可以使其成本和耗时降到很低且保持稳定，
不受数据点密集度的影响
优化点：
1、compaction put进入新的family 'n'
2、compaction delete整个旧的family 't'
3、查询去掉family限制（原先只查family 't'）

注意：由于数据在HBase中的存储格式改变，升级上线后，可以完全兼容老的数据格式，但原来的opentsdb版本不兼容新的数据格式，
     一旦回退，上线之后的这批数据通过原有版本的opentsdb是查不出来的
     以上是建立在compaction开启的情况下，如果没有开启就没有影响